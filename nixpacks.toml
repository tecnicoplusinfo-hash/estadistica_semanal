[phases.build]
cmds = [
  "cd backend && npm install",
  "cd backend && npx prisma generate",
  "cd frontend && npm install",
  "cd frontend && npm run build"
]

[phases.start]
cmds = ["cd backend && npm start"]

[static]
# Frontend será servido por Express en producción

[variables]
NODE_ENV = "production"
